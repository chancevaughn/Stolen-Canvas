<div class="card">
    <div class="card-image"><img class="materialboxed" src="{{{thumbnail}}}"></div>
    <div class="card-content">
        <h3>{{{title}}}</h3>
        <p>${{{value}}}</p>
    </div>
    <div class="card-action">
        <a id="deleteButton" class="black-text" href="#" data-id="{{{product_id}}}">Delete</a>
    </div>
</div>